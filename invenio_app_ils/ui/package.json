{
  "name": "ui",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build-main": "cd ./main && npm install && npm run build",
    "build-backoffice": "cd ./backoffice && npm install && npm run build",
    "build": "npm-run-all -p build-main build-backoffice"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
